---
# file: boxes.yml
#

vagrant:

  # Ubuntu

  trusty64.vagrant.dev:
    enabled: true
    box: ubuntu/trusty64
    network:
      name: private_network
      ip: 192.168.124.4

    provider:
      virtualbox:
        memory: 1024
        cpus: 1

